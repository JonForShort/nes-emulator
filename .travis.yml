dist: xenial
sudo: false
language:
  - cpp
compiler:
  - gcc
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc
      - cmake
      - libasound2-dev
script:
  - mkdir build
  - pushd build
  - cmake ..
  - make && make test
  - popd
